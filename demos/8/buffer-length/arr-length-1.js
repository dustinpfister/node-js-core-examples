let str = 'jalapeno';
console.log(str); // 'jalapeno'

// the string length is 8
console.log(str.length); // 8

// the buffer length is also 8
let buff = Buffer.from(str);
console.log(buff.length); // 8
